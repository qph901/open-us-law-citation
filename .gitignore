# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Downloaded dataset snapshots (large, gated; reproduce via scripts/download.py)
data/

# DuckDB spill files (identity_collisions default --temp-dir)
.duckdb_spill/

# Test, type-check, and lint caches
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Full discrepancy manifests (one JSON record per official provision, ~136 MB for the
# CFR). Regenerable byte-for-byte from the checksum-pinned oracle + dataset recorded in
# the committed Markdown scorecard, so the scorecard is the artifact under review and the
# manifest is a working file. Reproduce with `coverage-baseline compare`.
reports/COV-1A_*_discrepancies.json
